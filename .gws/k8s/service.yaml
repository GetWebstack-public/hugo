apiVersion: v1
kind: Service
metadata:
  name: hugo
  namespace: hugo
  labels:
    app: hugo
spec:
  type: ClusterIP
  selector:
    app: hugo
  ports:
  - port: 80
    targetPort: 1313
    protocol: TCP
    name: http
